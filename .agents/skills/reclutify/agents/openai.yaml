interface:
  display_name: "Reclutify"
  short_description: "Repo-specific patterns and workflows for reclutify"
  default_prompt: "Use the reclutify repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true